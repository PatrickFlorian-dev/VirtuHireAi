import React from "react";
//import { useAuth } from "../hooks/useAuth";
import EmailForm from "../../messaging/SendEmail";

const Demo: React.FC = () => {
  //const { user } = useAuth();

  return (
    <EmailForm />
  );
};

export default Demo;
